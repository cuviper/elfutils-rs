// imports to `include!` into bindgen's output

extern crate libc;

use libc::size_t;
use libc::{int32_t, int64_t};
use libc::{uint16_t, uint32_t, uint64_t};
use libc::{time_t, uid_t, gid_t, mode_t};
